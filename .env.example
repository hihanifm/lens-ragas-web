# Copy this file to .env and fill in your values.
# Never commit .env — it is gitignored.

# ── LLM judge ────────────────────────────────────────────────────────────────

# Provider: ollama (on-prem, default) | openai (testing only)
LLM_PROVIDER=ollama

# Ollama runs on the HOST machine outside Docker.
# host.docker.internal is mapped to the host automatically via extra_hosts.
# If Ollama runs on a separate GPU server, put that server's IP here.
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.2

# Only used when LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── Lab forward proxy ────────────────────────────────────────────────────────
# If Docker builds fail with connection errors (pip / npm can't reach the
# internet), your lab proxy needs to be declared explicitly.
# Set these before running make build — docker-compose picks them up from .env.
#
# HTTP_PROXY=http://proxy.lab.local:8080
# HTTPS_PROXY=http://proxy.lab.local:8080
# NO_PROXY=localhost,127.0.0.1,host.docker.internal

# ── Lab forward proxy / SSL inspection ───────────────────────────────────────
# If your lab runs a forward proxy that does SSL inspection (replaces TLS certs
# with its own CA), Docker pip and npm installs will fail with SSL errors.
# Fix: drop your lab's CA certificate into the certs/ folder before building.
#
#   cp /path/to/lab-ca.crt certs/lab-ca.crt
#   make build   (or make prod-build)
#
# Any *.crt file found in certs/ is automatically installed into the image's
# trust store. The folder is safe to leave empty — the build works either way.
# You may commit the .crt file; CA certs are not secrets.

# ── Deployment: reverse proxy sub-path ───────────────────────────────────────
# Scenario A — direct access (no reverse proxy):
#   Users hit http://<server-ip>:37100 directly.
#   Leave VITE_BASE_PATH unset (defaults to /).
#
# Scenario B — behind a reverse proxy with a sub-path:
#   Reverse proxy exposes the app at e.g. http://proxy-host/ragas/
#   and strips the prefix before forwarding to this container.
#   Set VITE_BASE_PATH to that prefix AT BUILD TIME so the frontend
#   knows where its assets and API calls live.
#   Example:
#     VITE_BASE_PATH=/lens-ragas-web make prod-up
#
# VITE_BASE_PATH=/lens-ragas-web
