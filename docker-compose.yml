services:
  api:
    build: ./backend
    ports:
      - "37100:37100"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backend/uploads:/app/uploads
